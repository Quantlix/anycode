name: Publish To PyPI

on:
  release:
    types:
      - published

concurrency:
  group: publish-pypi-${{ github.event.release.tag_name }}
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Set up uv
        uses: astral-sh/setup-uv@v7

      - name: Validate release tag against canonical version
        run: python scripts/check_versions.py --tag "${{ github.event.release.tag_name }}"

      - name: Install project and dev dependencies
        run: uv sync --group dev

      - name: Run release gate
        run: |
          uv run ruff check src/
          uv run ruff format --check src/
          uv run pyright
          uv run python -m pytest
          rm -rf dist/ build/
          uv run python -m build --no-isolation
          uv run twine check --strict dist/*

      - name: Verify built artifacts match release tag
        run: |
          tag="${{ github.event.release.tag_name }}"
          expected="anycode_py-${tag#v}"
          ls -l dist/
          test -f "dist/${expected}-py3-none-any.whl"
          test -f "dist/${expected}.tar.gz"

      - name: Upload distributions
        uses: actions/upload-artifact@v7
        with:
          name: python-package-distributions
          path: dist/

  publish:
    needs:
      - build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/anycode-py
    permissions:
      id-token: write

    steps:
      - name: Download distributions
        uses: actions/download-artifact@v8
        with:
          name: python-package-distributions
          path: dist/

      - name: Publish distributions to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          print-hash: true
