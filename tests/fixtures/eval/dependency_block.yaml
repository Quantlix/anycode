name: dependency_block
description: Agent reaches max_turns when no terminal answer is produced — exposes dependency-style failure.
prompt: Keep thinking out loud without finishing.
deterministic: true
fake_responses:
  - "Step 1: thinking..."
  - "Step 2: still thinking..."
expected_stop_reason: max_turns
max_turns: 2
model: fake-model
